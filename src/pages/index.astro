---
export const prerender = true;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>AmicBridge — Private lending & business partnerships</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="A private, trusted bridge between borrowers, lenders, and business partners. AmicBridge never holds funds or signs contracts." />

    <style>
      :root {
        --bg: #ffffff;
        --text: #0f172a;
        --muted: #475569;
        --line: #e2e8f0;
        --blue: #2563eb;
        --soft: #f8fafc;
        --radius: 16px;
        --shadow: 0 10px 30px rgba(2,6,23,.08);
        --sans: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        font-family: var(--sans);
        background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%);
        color: var(--text);
      }

      header {
        border-bottom: 1px solid var(--line);
        background: #fff;
      }

      .nav {
        max-width: 1100px;
        margin: 0 auto;
        padding: 14px 18px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 16px;
      }

      .brand {
        font-weight: 800;
        letter-spacing: -.02em;
        font-size: 20px;
      }

      .nav a {
        text-decoration: none;
        color: var(--muted);
        font-weight: 600;
        margin-left: 16px;
      }

      .nav a:hover {
        color: var(--text);
      }

      main {
        max-width: 1100px;
        margin: 0 auto;
        padding: 60px 18px 80px;
      }

      h1 {
        font-size: clamp(34px, 4vw, 56px);
        line-height: 1.05;
        letter-spacing: -.02em;
        margin: 0 0 20px;
      }

      p.lead {
        max-width: 70ch;
        font-size: 16px;
        line-height: 1.7;
        color: var(--muted);
        margin-bottom: 32px;
      }

      .actions {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        margin-bottom: 50px;
      }

      .btn {
        padding: 14px 18px;
        border-radius: 14px;
        border: 1px solid var(--line);
        font-weight: 700;
        cursor: pointer;
        text-decoration: none;
        background: #fff;
        color: var(--text);
      }

      .btn.primary {
        background: var(--blue);
        color: #fff;
        border-color: var(--blue);
      }

      .btn:hover {
        transform: translateY(-1px);
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: 20px;
      }

      .card {
        background: #fff;
        border: 1px solid var(--line);
        border-radius: var(--radius);
        padding: 22px;
        box-shadow: var(--shadow);
      }

      .card h3 {
        margin: 0 0 10px;
        font-size: 18px;
      }

      .card p {
        margin: 0;
        font-size: 14px;
        line-height: 1.6;
        color: var(--muted);
      }

      footer {
        border-top: 1px solid var(--line);
        background: var(--soft);
        padding: 26px 18px;
        font-size: 12px;
        color: var(--muted);
      }

      footer .inner {
        max-width: 1100px;
        margin: 0 auto;
        line-height: 1.6;
      }
    </style>
  </head>

  <body>
    <header>
      <div class="nav">
        <div class="brand">AmicBridge</div>
        <nav>
          <a href="/how-it-works">How it works</a>
          <a href="/trust">Trust profile</a>
          <a href="/loan-agreement">Loan agreement</a>
          <a href="/contact">Contact</a>
        </nav>
      </div>
    </header>

    <main>
      <h1>A private, trusted bridge between borrowers, lenders, and business partners.</h1>

      <p class="lead">
        AmicBridge is a private network that connects people who want to lend,
        borrow, or collaborate on their own terms. We do not control deals,
        hold funds, or sign contracts — we help you find real people and build
        long-term financial relationships.
      </p>

      <div class="actions">
        <a class="btn primary" href="/request-loan">Request a loan</a>
        <a class="btn" href="/offer-lending">Offer lending</a>
        <a class="btn" href="/loan-agreement">View loan agreement</a>
      </div>

      <section class="grid">
        <div class="card">
          <h3>Direct agreements</h3>
          <p>
            All agreements are made directly between members. AmicBridge is not
            a lender, borrower, broker, or escrow.
          </p>
        </div>

        <div class="card">
          <h3>No custody</h3>
          <p>
            We never hold fiat, crypto, or assets. Transfers happen directly
            between parties.
          </p>
        </div>

        <div class="card">
          <h3>Long-term trust</h3>
          <p>
            Build relationships that can evolve from loans into shared profit
            or business partnerships.
          </p>
        </div>
      </section>
    </main>

    <footer>
      <div class="inner">
        AmicBridge never holds funds or crypto, never signs loan or investment
        contracts, and never guarantees repayment, returns, or matches.
        All agreements and transfers happen directly between members, who are
        responsible for their own decisions, compliance, and due diligence.
      </div>
    </footer>
  </body>
</html>

